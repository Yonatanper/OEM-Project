// Load dependencies.
var simple_timer = require('../lib')
var http = require('http')

// Start timer.
simple_timer.start('my_timer')
http.get('http://example.com', function(res) {
    // Stop timer.
    simple_timer.stop('my_timer')
    simple_timer.remove('my_timer')
})
